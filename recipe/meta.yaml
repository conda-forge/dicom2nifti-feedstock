{% set name = "dicom2nifti" %}
{% set version = "2.3.4" %}
{% set sha256 = "2bedcd481f30081998b4fc8536434c46bf5f7dd6ee9067e3070c476437f78012" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/icometrix/{{ name }}/archive/{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  noarch: python
  number: 0
  script: python -m pip install --no-deps --ignore-installed .

requirements:
  build:
    - python
    - pip
    - setuptools
  run:
    - python
    - pydicom
    - numpy
    - nibabel
    - scipy

test:
  imports:
    - dicom2nifti
  requires:
    - setuptools
  
about:
  home: http://github.com/icometrix/dicom2nifti
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: 'Python library for converting dicom files to nifti'
  description: 'Python library for converting dicom files to nifti'
  doc_url: https://icometrix.github.io/dicom2nifti/
  dev_url: http://github.com/icometrix/dicom2nifti

extra:
  recipe-maintainers:
    - abrys
